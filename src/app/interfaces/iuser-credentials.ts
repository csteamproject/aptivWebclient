/**
 * IUserCredentials is the interface of what the user logs in with and only exists separetly on the Angular web portal.
 */
export interface IUserCredentials {
    username: string;
    password: string;
}
